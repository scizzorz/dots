[user]
	name = John Weachock
	email = jweachock@gmail.com

[core]
	pager = ~/dots/gitpager.sh

[color]
	ui = true
	branch = auto
	diff = auto
	status = auto

[color "branch"]
	current = cyan bold
	local = blue bold
	remote = red bold

[color "diff"]
	plain = white
	meta = cyan bold
	frag = cyan
	old = red
	new = green
	commit = black bold

[color "status"]
	added = green
	changed = blue bold
	untracked = cyan bold

[alias]
	unstage = reset HEAD
	us = reset HEAD
	st = status -s
	ci = commit
	br = branch
	co = checkout
	df = diff
	dfs = diff --staged

	top = log --pretty=format:'%C(bold red)%h%C(reset)%C(auto)%d%C(reset)' --abbrev-commit --all --graph --simplify-by-decoration --date-order
	l = log --pretty=format:'%C(bold red)%h%C(reset) %C(green)%ad %C(bold yellow)%an%C(reset)%C(auto)%d%C(reset) %s%C(reset)' --abbrev-commit --date-order --date=short

	ls = !git l --simplify-by-decoration
	la = !git l --all
	lg = !git l --graph

	las = !git l --all --simplify-by-decoration
	lgs = !git l --graph --simplify-by-decoration
	lga = !git l --graph --all

	lgas = !git l --graph --all --simplify-by-decoration
